# caiman_pipeline.yaml

# installs caiman and jobcreator for running miniscope image processing
# on the sciCORE cluster

name: caiman
channels:
  - conda-forge
  - default
dependencies:
  - python=3.7
  - pip
  - caiman
  - pip:
    - jobcreator==0.0.4
